# Nimbus Handbook

## Storage quotas

Each project has a storage quota of 50 GB by default.
The quota can be raised by contacting support.

## Error codes

The error NIMBUS_QUOTA_EXCEEDED is returned when a project exceeds its quota.
